Configuration management
========================

Org-wide and pipeline-level defaults and wiring — framework configuration,
mandatory table properties, Spark configuration, operational metadata, secrets,
logging, and build parallelization.

.. toctree::
   :maxdepth: 1

   Framework configuration <feature_framework_configuration>
   Mandatory Table Properties <feature_mandatory_table_properties>
   Spark Configuration <feature_spark_configuration>
   Operational Metadata <feature_operational_metadata>
   Secrets Management <feature_secrets>
   Logging <feature_logging>
   Builder Parallelization <feature_builder_parallelization>
