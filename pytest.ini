[pytest]
testpaths = tests
pythonpath = src tests
markers =
    spark: tests requiring a local Spark session
    integration: tests that depend on samples or external layout
    bdd: pytest-bdd scenario tests
