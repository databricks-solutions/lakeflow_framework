resources:
  pipelines:
    lakeflow_pattern_silver_stream_static_streaming_dwh_pipeline:
      name: Lakeflow Framework - Pattern - Silver Stream Static Streaming DWH Pipeline (${var.logical_env})
      channel: CURRENT
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.silver_schema}
      libraries:
        - notebook:
            path: ${var.framework_source_path}/dlt_pipeline
      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
        bundle.target: ${bundle.target}
        framework.sourcePath: ${var.framework_source_path}
        workspace.host: ${var.workspace_host}
        pipeline.layer: silver
        logicalEnv: ${var.logical_env}
        pipeline.dataFlowGroupFilter: stream_static_samples
      root_path: ${workspace.file_path}/src
