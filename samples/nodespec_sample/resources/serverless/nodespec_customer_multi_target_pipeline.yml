resources:
  pipelines:
    lakeflow_samples_nodespec_customer_multi_target:
      name: Lakeflow Framework - Nodespec - Customer Multi Target (${var.logical_env})
      channel: CURRENT
      serverless: true
      catalog: ${var.catalog}
      schema: ${var.schema}
      libraries:
        - notebook:
            path: ${var.framework_source_path}/dlt_pipeline

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
        bundle.target: ${bundle.target}
        framework.sourcePath: ${var.framework_source_path}
        workspace.host: ${var.workspace_host}
        pipeline.layer: ${var.layer}
        logicalEnv: ${var.logical_env}
        pipeline.dataFlowIdFilter: nodespec_customer_multi_target
      root_path: ${workspace.file_path}/src/dataflows/base_samples

