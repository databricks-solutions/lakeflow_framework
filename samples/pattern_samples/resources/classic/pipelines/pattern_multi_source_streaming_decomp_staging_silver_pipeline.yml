resources:
  pipelines:
    pattern_multi_source_streaming_decomp_staging_silver_pipeline:
      name: Lakeflow Framework - Pattern - Multi Source Streaming Decomposed Staging - Silver (${var.logical_env})
      channel: CURRENT
      clusters:
        - ${var.pipeline_cluster_config}
      catalog: ${var.catalog}
      schema: ${var.silver_schema}
      libraries:
        - notebook:
            path: ${var.framework_source_path}/dlt_pipeline
      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
        bundle.target: ${bundle.target}
        framework.sourcePath: ${var.framework_source_path}
        workspace.host: ${var.workspace_host}
        pipeline.layer: silver
        logicalEnv: ${var.logical_env}
        pipeline.dataFlowGroupFilter: multi_source_streaming_decomposed_staging
      root_path: ${workspace.file_path}/src
